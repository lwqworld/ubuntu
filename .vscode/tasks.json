{
    "tasks": [
        {
            "type": "shell",
            "label": "C/C++: g++ build active file",
            "command": "g++ `pkg-config --cflags opencv4` ${file}  -o ${fileDirname}/${fileBasenameNoExtension} `pkg-config --libs opencv4`",
            "args": [
                

            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}